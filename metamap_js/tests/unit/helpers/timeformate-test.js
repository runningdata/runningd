import { timeformate } from 'metamap/helpers/timeformate';
import { module, test } from 'qunit';

module('Unit | Helper | timeformate');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = timeformate([42]);
  assert.ok(result);
});
