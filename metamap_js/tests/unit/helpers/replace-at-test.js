import { replaceAt } from 'metamap/helpers/replace-at';
import { module, test } from 'qunit';

module('Unit | Helper | replace at');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = replaceAt([42]);
  assert.ok(result);
});
