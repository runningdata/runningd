<?xml version="1.0" encoding="UTF-8"?>
<beans
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.2.xsd http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-3.2.xsd http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-3.2.xsd http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.2.xsd http://www.springframework.org/schema/data/mongo http://www.springframework.org/schema/data/mongo/spring-mongo-1.0.xsd http://www.springframework.org/schema/task http://www.springframework.org/schema/task/spring-task-3.1.xsd"
	xmlns:context="http://www.springframework.org/schema/context" xmlns:mongo="http://www.springframework.org/schema/data/mongo" xmlns:tx="http://www.springframework.org/schema/tx" xmlns:aop="http://www.springframework.org/schema/aop"
	xmlns:p="http://www.springframework.org/schema/p" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://www.springframework.org/schema/beans" xmlns:task="http://www.springframework.org/schema/task">

	<!-- hive meta库 -->
	<bean id="hivemeta" class="com.mchange.v2.c3p0.ComboPooledDataSource">
        <property name="driverClass" value="com.mysql.jdbc.Driver" />
        <property name="jdbcUrl" value="${jdbc.hivemeta.url}" />
        <property name="user" value="${jdbc.hivemeta.username}" />
        <property name="password" value="${jdbc.hivemeta.password}" />
        <property name="initialPoolSize" value="${jdbc.hivemeta.initialPoolSize}" />
        <property name="minPoolSize" value="${jdbc.hivemeta.minPoolSize}" />
        <property name="maxStatements" value="0" />
        <property name="maxStatementsPerConnection" value="100"></property>
        <property name="maxPoolSize" value="${jdbc.hivemeta.maxPoolSize}" />
        <property name="maxIdleTime" value="${jdbc.hivemeta.maxIdleTime}" />
        <property name="idleConnectionTestPeriod" value="${jdbc.hivemeta.idleConnectionTestPeriod}" />
    </bean>
	
	<!-- hive server库 -->
    <bean id="hiveserver" class="com.mchange.v2.c3p0.ComboPooledDataSource">
        <property name="driverClass" value="org.apache.hive.jdbc.HiveDriver" />
        <property name="jdbcUrl" value="${jdbc.hiveserver.url}" />
        <property name="user" value="${jdbc.hiveserver.username}" />
        <property name="password" value="${jdbc.hiveserver.password}" />
        <property name="initialPoolSize" value="${jdbc.hiveserver.initialPoolSize}" />
        <property name="minPoolSize" value="${jdbc.hiveserver.minPoolSize}" />
        <property name="maxStatements" value="0" />
        <property name="maxStatementsPerConnection" value="100"></property>
        <property name="maxPoolSize" value="${jdbc.hiveserver.maxPoolSize}" />
        <property name="maxIdleTime" value="${jdbc.hiveserver.maxIdleTime}" />
        <property name="idleConnectionTestPeriod" value="${jdbc.hiveserver.idleConnectionTestPeriod}" />
    </bean>

	<bean id="hivemetaJdbcTemplate" class="org.springframework.jdbc.core.JdbcTemplate">
		<property name="dataSource" ref="hivemeta" />
	</bean>

</beans>