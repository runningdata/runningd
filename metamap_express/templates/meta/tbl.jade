extends ../layout

block content
  head
    title 表信息
    
  h2 表信息

  | #{tbl.createTime}
  
  if cols.length
    table.table.table-striped
      caption 表信息 : #{tbl.TBL_NAME}
        thead 
          tr
            td ID
            td 名称
            td 类型
      tbody
        each col in cols
          tr
            td #{col.id}
            td #{col.colName}
            td #{col.colTypeName}